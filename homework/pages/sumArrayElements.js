
    var sum = 0;
    var numbers = [-2, 3, 7, 8, -1, 4, 2];
    for ( var i = 0; i < numbers.length ; i++ ){
        var sum=numbers[i];
        sum += i ;
    };

    if ((sum % 2 === 0 )&& (sum > 0)){
        console.log(`sum =${sum} `);

    };





